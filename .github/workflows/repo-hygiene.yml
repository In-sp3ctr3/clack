name: Repository Hygiene

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  hygiene:
    name: Required files and conflict checks
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Check required community files
        shell: bash
        run: |
          set -euo pipefail

          required=(
            "README.md"
            "LICENSE"
            "CONTRIBUTING.md"
            "CODE_OF_CONDUCT.md"
            "SECURITY.md"
            "SUPPORT.md"
            "GOVERNANCE.md"
            "CHANGELOG.md"
            ".github/PULL_REQUEST_TEMPLATE.md"
            ".github/CODEOWNERS"
            ".github/workflows/pr-discipline.yml"
          )

          missing=0
          for file in "${required[@]}"; do
            if [[ ! -f "$file" ]]; then
              echo "Missing required file: $file"
              missing=1
            fi
          done

          exit "$missing"

      - name: Check for committed merge conflict markers
        shell: bash
        run: |
          set -euo pipefail

          if git grep -nE '^(<<<<<<<|=======|>>>>>>>)' -- .; then
            echo "Merge conflict markers were found."
            exit 1
          fi

  swift:
    name: Swift checks
    runs-on: macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run core checks
        run: swift run ClackCoreChecks

      - name: Build app
        run: swift build --configuration release

      - name: Package app
        run: BUILD_UNIVERSAL=0 ./scripts/build_app.sh
